
jQuery('.modal_form').append('<%= escape_javascript(render "edit_form") %>');

jQuery('#editModal').modal('show');

 jQuery('body').on('hidden.bs.modal', '#editModal', function () {
  jQuery(this).remove();
});